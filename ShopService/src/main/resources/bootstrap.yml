#endpoints.health.enabled=true
#endpoints.health.sensitive=false

spring:
  application:
    name: shopService # fixme: why does application.name not work???
  cloud:
    consul:
      discovery:
        instanceid: ${spring.application.name}:${spring.application.instance_id:${random.value}}
        prefer-ip-address: true
        health-check-critical-timeout: 1m
      host: localhost
      port: 8500


endpoints:
  health:
    enabled: true
    sensitive: false

---
spring:
  profiles: docker
  cloud:
    consul:
      host: consul
server:
  port: 8080