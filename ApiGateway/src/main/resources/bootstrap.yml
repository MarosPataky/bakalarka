#endpoints.health.enabled=true
#endpoints.health.sensitive=false

spring:
  application:
    name: apigateway # fixme: why does application.name not work???
  cloud:
    consul:
      discovery:
        instanceid: ${spring.application.name}:${spring.application.instance_id:${random.value}}
        prefer-ip-address: true
      host: localhost
      port: 8500


endpoints:
  health:
    enabled: true
    sensitive: false

---
spring:
  profiles: docker
  cloud:
    consul:
      host: consul
server:
  port: 8080